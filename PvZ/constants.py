import random as rn # Central random used for various randomization. disconnected from the seeded randomization used for matches.

project_name = "ProjectPvZ"
frame_rate = 60
screen_size = (1600,900)
dev_mode = False # debug tools, such as drawing rects for most surfaces
board_proportions = [300, 390, 1200, 440]
display_proportions = [525, 20, 950, 300]