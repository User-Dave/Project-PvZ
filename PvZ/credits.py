import textgen
import pygame
import OnlineMatch
pygame.init()
scr = pygame.display.set_mode((100,100))
def main():
    # font = textgen.font([(120,100,120),(160,120,160)],[88,88])
    # rendered_surface = font.render("CREDITS:")
    # pygame.image.save(rendered_surface,'cr.png')

    # font = textgen.font([(166,100,166),(222,150,222)])
    # rendered_surface = font.render("YAN M$VSH$VICH\nPROGRAMMING CONSULTING.")
    # pygame.image.save(rendered_surface,'yan.png')

    # font = textgen.font([(222,222,100),(230,230,170)])
    # rendered_surface = font.render("ITAMAR RAPAP#RT\nPROGRAMMING,ART,DESIGN.")
    # pygame.image.save(rendered_surface,'itipi.png')

    # font = textgen.font([(170,75,75),(222,100,100)])
    # rendered_surface = font.render("REVITAL NAH^N SHERINSKY\nWRITING/DESIGN CONSULTING.")
    # pygame.image.save(rendered_surface,'revital.png')

    print("done")

if __name__ == "__main__":
    main()